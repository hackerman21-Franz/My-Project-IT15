@{
    ViewBag.Title = "Payment Failed";
}

<div class="container py-5 text-center">
    <div class="card p-4 shadow-sm rounded-4">
        <h2 class="text-success fw-bold mb-3">Payment Failed</h2>
        <p class="lead">Your payment has been cancelled.</p>
        <a class="btn btn-primary mt-4" asp-controller="Meter" asp-action="UserBillings">
            Back to My Bills
        </a>
    </div>
</div>